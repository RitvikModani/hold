# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# secrets
.env

# The harvested pool IS committed. It is generated, but it is also the app's
# content — without it a deployed build has nothing to play. ~310K, 41K gzipped.
# Re-run `npm run harvest` to refresh it.
